# Docker Tutorial



Docker client-server application. The client is our command-line and the server is the daemon running in our computer.

Falar sobre Docker:

1. Motivação em Python? Dependências
2. Criando um Dockerfile
3. Diferenciar Volume e Copy
4. Mostrar a utilidade de um gitignore (espaço que ocupa)
5. Docker build
6. Docker run, modo interativo
7. Docker Compose, rodando várias instâncias

Aplicação em Python que faz parse da página dada e salva o parsing em um txt, fazer isso para três containers.





## Docker Build

Mostrar que se não colocar a tag -t, docker ps não mostra o id nem nada.